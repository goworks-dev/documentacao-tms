import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

# Ambientes API

Os ambientes disponíves no AbverbGo são os de `Produção(PRD)` e de `Qualidade(QA)`

Links para os ambientes:

- PRD `https://api.averbgo.com.br/v1/`
- QA `https://api.qa.averbgo.com.br/v1/`
